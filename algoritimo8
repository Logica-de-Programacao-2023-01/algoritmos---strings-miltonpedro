package main

import "fmt"

func main() {
	var str string
	fmt.Print("Digite uma string:")
	fmt.Scanln(&str)

	resultado := ""
	for i := len(str) - 1; i >= 0; i-- {
		resultado += string(str[i])
	}

	fmt.Println("A nova string é:", resultado)
}
